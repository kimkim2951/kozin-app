.tweet-create
  .d-flex.align-items-center
    %h1 投稿詳細
    .ml-auto.posts_button
      %a.btn.btn-outline-info{href: "/"} 投稿一覧 
    .card.mb-4.shadow-
      .content_post{style: "background-image: url(#{@tweet.image});"}
    .form-group
      - if current_user
        = form_with(model: [@tweet, @comment], local: true) do |f|
          %label ＜コメント投稿＞
          = f.text_area :text, class: "Textarea1 form-control", rows: "3", placeholder: "コメントする"
          %small#emailHelp.form-text.text-muted コメントを入力してください。
          = f.submit nil, class: "btn btn-primary"
      - else
        %strong
          %p ~~~ コメントを投稿するには新規登録／ログインが必要です ~~~
      .comments
        %label ＜コメント一覧＞
        - if @comments
          - @comments.each do |comment|
            %p
              %strong
                = link_to comment.user.name, "/users/#{comment.user_id}"
              = comment.text